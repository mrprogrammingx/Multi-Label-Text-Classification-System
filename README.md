# Multi-Label Text Classification System

This repository contains code and resources for a multi-label text classification system.

Important folders
- `src/` - source code for the project
- `notebooks/` - exploratory notebooks and experiments

Ignored folders
- `models/` - trained model artifacts are excluded from version control and listed in `.gitignore`.
- `data/raw/` - raw datasets are large and kept outside the repository; `data/` is in `.gitignore`.

Datasets

Download the datasets and place them under `data/raw/` (created locally, not committed):

- dataset_1: https://drive.google.com/file/d/1XkDyylgWw_tOaHU2eRpyFnG_8-eJYXj9/view?usp=drive_link
- dataset_1_val: https://drive.google.com/file/d/1OInznXIqoga09PUK4LCbnLlZr8g7ubna/view?usp=drive_link
- dataset_1_train: https://drive.google.com/file/d/1_XaoLfgX23qoX5RSBQqL0Qc9TfTyqrC2/view?usp=drive_link

## Short description

This repository implements an end-to-end multi-label text classification system for moderation-related labels on online comments. The pipeline covers data loading, text cleaning, TF‑IDF vectorization, multi‑label model training, per‑label evaluation, error analysis and saving artifacts for inference.

Keywords: moderation, multi-label, TF-IDF, sklearn, text-classification

Features
- End-to-end pipeline: load → preprocess → train → evaluate → save
- TF‑IDF vectorization and One‑vs‑Rest logistic regression baseline
- Per‑label evaluation and simple error analysis
- Save/load of model and preprocessing artifacts with joblib

Tech stack
- Python 3.10+
- Jupyter notebooks for exploration
- pandas, numpy
- scikit-learn
- matplotlib (for plotting)
- joblib (model persistence)

Datasets

Download the datasets and place them in the repository under `data/raw/`. The project expects the following files (rename the downloads if necessary):

- `data/raw/dataset_C_train.csv`  (training table)
- `data/raw/dataset_C_val.csv`    (validation table)
- `data/raw/dataset_C.csv`        (optional full dataset)

Original download links (rename to the filenames above):

- https://drive.google.com/file/d/1XkDyylgWw_tOaHU2eRpyFnG_8-eJYXj9/view?usp=drive_link
- https://drive.google.com/file/d/1OInznXIqoga09PUK4LCbnLlZr8g7ubna/view?usp=drive_link
- https://drive.google.com/file/d/1_XaoLfgX23qoX5RSBQqL0Qc9TfTyqrC2/view?usp=drive_link

Dataset format / schema
- One `text` column (string) containing the comment.
- Multiple binary label columns (each 0/1) indicating presence of a moderation label. Example columns: `toxic`, `spam`, `personal_attack`, `is_empty_after_clean`.

- One `text` column (string) containing the comment.
- Multiple binary label columns (each 0/1) indicating presence of a moderation label. Example columns: `toxic`, `spam`, `personal_attack`.

Note: some datasets may include helper/debug columns (for example `is_empty_after_clean`) that are not moderation labels. These helper columns should be excluded from the label set when training/evaluating (the pipeline already ignores constant or helper columns).

Example CSV header (comma-separated):

```
text,label_1,label_2,label_3,label_4
"this is a comment",0,0,1,0
```

Note: the supplied dataset uses generic column names `label_1`..`label_4` for the moderation labels. If you prefer, map these to human-readable names in your notebooks or evaluation reports (e.g. `label_1 -> toxic`).

Setup (local)
1. Create and activate a virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

2. Install dependencies:

```bash
pip install -r requirements.txt
```

How to run
1. Make sure the dataset files are present in `data/raw/` (see filenames above).
2. Run the pipeline (this executes load → preprocess → train → evaluate → save):

```bash
python src/main.py
```

Note: `python -m src.main` runs a module as a package. That form requires `src` to be an importable package (for example by adding `src/__init__.py`) and for the `src` parent directory to be on Python's path. The direct runner above (`python src/main.py`) works regardless and is the recommended command for this repository unless you intentionally convert `src` into a package.

Notes
- The pipeline will detect and ignore any label columns that are constant in the training set (no variance). These cannot be learned by the model and are skipped with a warning.
- Models and vectorizers are saved under `models/` as `model.joblib` and `vectorizer.joblib`.

Expected outputs
- `models/model.joblib` — trained multi-label model
- `models/vectorizer.joblib` — TF‑IDF vectorizer used for training
- Console output with per‑label precision/recall/F1
- (Optional) `reports/` or notebook plots if generated by evaluation code

Project structure

```
.
├─ data/raw/                # raw datasets (ignored)
├─ notebooks/               # exploratory notebooks
├─ src/                     # source modules and entrypoint
├─ models/                  # trained artifacts (generated at runtime)
├─ requirements.txt
└─ README.md
```

Team / roadmap (optional)
- Member roles and week‑by‑week checklist (kept here for project management). Move to docs/ if you prefer.

